<!DOCTYPE html>
<html>
<head>
  <title>Weather Forecaster</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header"><%= link_to("<h2>Weather Forecaster</h2>".html_safe, forecasts_path) %></div>
    <% if user_signed_in? %>
      <%= link_to "Sign Out", destroy_user_session_path, {method: :delete, class: "navbar-text navbar-right"} %>
        <p class="navbar-text navbar-right">Welcome, <%= current_user.email %>!</p> 
      <% else %>
        <%= link_to "Sign In", new_user_session_path, {class: "navbar-text navbar-right"} %>
      <% end %>
  </div>
</nav>

<% flash.each do |key, value| %>
  <div class="alert <%= flash_class(key) %>"><%= value %></div>
<% end %>

<div class="container">
<%= yield %>
</div>

</body>
</html>
